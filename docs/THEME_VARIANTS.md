# 🎨 Варианты цветовых тем

Палитра из видео Hero-секции с двумя вариантами оформления.

## 📋 Базовая палитра

```css
--carrot-orange: #f99f37  /* Морковно-оранжевый */
--light-yellow: #fcfada   /* Светло-жёлтый */
--olive-bark: #534a1b     /* Оливково-коричневый */
--charcoal-blue: #3a4756  /* Угольно-синий */
--rosy-copper: #c95240    /* Розово-медный */
--espresso: #511809       /* Эспрессо (тёмно-коричневый) */
```

---

## Вариант 1: Тёмная тема ⭐ (Текущая)

**Концепция:** Тёмная подложка с яркими золотыми карточками

### CSS переменные:

```css
:root {
  --primary-red: #c95240;        /* rosy-copper - основной акцент */
  --deep-burgundy: #511809;      /* espresso - тёмный фон */
  --cream-bg: #511809;           /* espresso - фон страниц */
  --dark-green: #534a1b;         /* olive-bark - акценты */
  --golden-brown: #f99f37;       /* carrot-orange - золотые элементы */
  --text-dark: #fcfada;          /* light-yellow - светлый текст на тёмном */
  --text-light: #fcfada;         /* light-yellow - светлый текст */
  --card-bg: #fcfada;            /* light-yellow - светлые карточки */
  --charcoal-blue: #3a4756;      /* neutral accent */
}
```

### Применение:

- **Фон страниц:** `#511809` (espresso) - глубокий тёмный
- **Карточки:** `#fcfada` (light-yellow) - светлые, контрастные
- **Текст на фоне:** `#fcfada` (light-yellow) - светлый
- **Текст на карточках:** `#511809` (espresso) - тёмный
- **Акценты/CTA:** `#f99f37` (carrot-orange) - золотистые кнопки
- **Границы:** `#c95240` (rosy-copper) - медно-красные

### Визуальный эффект:

✨ Элегантная тёмная тема с яркими золотыми карточками, создающими ощущение тепла и роскоши

---

## Вариант 2: Светлая тема

**Концепция:** Светлая подложка с золотисто-красными карточками

### CSS переменные:

```css
:root {
  --primary-red: #c95240;        /* rosy-copper - основной акцент */
  --deep-burgundy: #511809;      /* espresso - тёмные акценты */
  --cream-bg: #fcfada;           /* light-yellow - светлый фон */
  --dark-green: #534a1b;         /* olive-bark - акценты */
  --golden-brown: #f99f37;       /* carrot-orange - золотые элементы */
  --text-dark: #511809;          /* espresso - тёмный текст */
  --text-light: #fcfada;         /* light-yellow - светлый текст */
  --card-bg: #f99f37;            /* carrot-orange - золотые карточки */
  --card-bg-alt: #c95240;        /* rosy-copper - альтернативные карточки */
  --charcoal-blue: #3a4756;      /* neutral accent */
}
```

### Применение:

- **Фон страниц:** `#fcfada` (light-yellow) - светлый тёплый
- **Карточки:** `#f99f37` (carrot-orange) - золотистые
- **Альтернативные карточки:** `#c95240` (rosy-copper) - медно-красные
- **Текст на фоне:** `#511809` (espresso) - тёмный
- **Текст на карточках:** `#511809` (espresso) - тёмный
- **Header/Footer:** `#511809` (espresso) - тёмные с золотыми акцентами
- **Акценты:** `#534a1b` (olive-bark) - оливковые детали

### Визуальный эффект:

☀️ Светлая, солнечная тема с тёплыми золотисто-красными карточками, создающая ощущение энергии и оптимизма

---

## 🔧 Как переключить тему

### Вариант 1 → Вариант 2:

1. Откройте `public/static/styles.css`
2. Найдите блок `:root { ... }`
3. Замените значения переменных на те, что указаны в Варианте 2
4. Обновите цвета карточек:

```css
/* Карточки - Вариант 2 */
.floating-card {
  background: var(--golden-brown);  /* золотистые карточки */
  color: var(--deep-burgundy);     /* тёмный текст */
}

.project-card {
  background: var(--golden-brown);
  color: var(--deep-burgundy);
}

/* Альтернативные карточки */
.project-card:nth-child(even) {
  background: var(--primary-red);  /* медно-красные */
}
```

5. Обновите body:

```css
body {
  background-color: var(--cream-bg);  /* светлый фон */
  color: var(--text-dark);            /* тёмный текст */
}
```

6. Пересоберите:
```bash
npm run build && pm2 restart human-time
```

---

## 💡 Рекомендации

### Вариант 1 (Тёмная) подходит для:
- Вечернего/ночного просмотра
- Серьёзной, доверительной атмосферы
- Элегантного, премиального восприятия
- Фокуса на контенте карточек

### Вариант 2 (Светлая) подходит для:
- Дневного просмотра
- Энергичной, позитивной атмосферы
- Активного призыва к действию
- Яркого, запоминающегося впечатления

---

## 🎬 Hero-секция

Оба варианта используют одинаковую Hero-секцию с видео:

- **Видео:** `/hero-video.mp4`
- **Оверлей:** `rgba(0, 0, 0, 0.4)` - затемнение 40%
- **Заголовок:** "ВРЕМЯ ЧЕЛОВЕКА" - белый, крупный, по центру
- **Подзаголовок:** "— Благотворительный фонд —" - золотистый
- **Кнопки:** Primary (золотистая) + Outlined (белая обводка)

---

## 📦 Компоненты по темам

| Элемент | Вариант 1 (Тёмная) | Вариант 2 (Светлая) |
|---------|-------------------|---------------------|
| Body BG | `#511809` espresso | `#fcfada` light-yellow |
| Cards BG | `#fcfada` light-yellow | `#f99f37` carrot-orange |
| Text | `#fcfada` light | `#511809` dark |
| Buttons | `#f99f37` gold | `#c95240` copper |
| Header | `rgba(0,0,0,0.5)` | `#511809` espresso |
| Footer | `rgba(0,0,0,0.8)` | `#511809` espresso |
