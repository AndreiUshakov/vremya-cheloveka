# 🎨 Переключение между темами

## Текущая тема: ☀️ Вариант 2 - Светлая тема

### Активные цвета:
- **Фон страницы**: `#fcfada` (light-yellow) - тёплый светлый бежевый
- **Карточки**: `#f99f37` (carrot-orange) - золотистые
- **Альтернативные карточки**: `#c95240` (rosy-copper) - медно-красные
- **Текст на фоне**: `#511809` (espresso) - тёмный
- **Header/Footer**: `#511809` (espresso) - тёмно-коричневые
- **Акценты**: `#f99f37` (золотые кнопки)

---

## 🔄 Как переключиться на Вариант 1 (Тёмная тема)

### Шаг 1: Обновите переменные в `public/static/styles.css`

Замените в начале файла блок `:root` на:

```css
/* Цветовая палитра из видео - Вариант 1 (Тёмная тема) 🌙 */
:root {
  --primary-red: #c95240;        /* rosy-copper - основной акцент */
  --deep-burgundy: #511809;      /* espresso - тёмный фон */
  --cream-bg: #511809;           /* espresso - фон страниц */
  --dark-green: #534a1b;         /* olive-bark - акценты */
  --golden-brown: #f99f37;       /* carrot-orange - золотые элементы */
  --text-dark: #fcfada;          /* light-yellow - светлый текст на тёмном */
  --text-light: #fcfada;         /* light-yellow - светлый текст */
  --card-bg: #fcfada;            /* light-yellow - светлые карточки */
  --charcoal-blue: #3a4756;      /* neutral accent */
}
```

### Шаг 2: Обновите body цвет текста

Найдите:
```css
body {
  ...
  color: var(--text-dark);
  ...
}
```

Замените на:
```css
body {
  ...
  color: var(--text-light);
  ...
}
```

### Шаг 3: Обновите header

Найдите:
```css
.site-header {
  background-color: var(--deep-burgundy);
  ...
}
```

Замените на:
```css
.site-header {
  background-color: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(10px);
  ...
}

.site-header.scrolled {
  background-color: rgba(81, 24, 9, 0.95);
  ...
}
```

### Шаг 4: Уберите альтернативные карточки

Удалите или закомментируйте:
```css
/* Альтернативные карточки - медно-красные */
.project-card:nth-child(even) {
  background: var(--card-bg-alt);
  color: var(--text-light);
}

.project-card:nth-child(even):hover {
  box-shadow: 0 15px 40px rgba(201, 82, 64, 0.5);
}
```

### Шаг 5: Пересоберите проект

```bash
cd /home/user/webapp
npm run build
pm2 restart human-time
```

---

## 🔄 Как вернуться на Вариант 2 (Светлая тема)

### Выполните обратные операции:

1. Замените `:root` блок на светлую палитру (см. начало файла)
2. Измените `color: var(--text-light)` → `color: var(--text-dark)` в `body`
3. Обновите header на `background-color: var(--deep-burgundy)`
4. Добавьте обратно альтернативные карточки
5. Пересоберите: `npm run build && pm2 restart human-time`

---

## 📊 Сравнение тем

| Элемент | Вариант 1 (Тёмная) 🌙 | Вариант 2 (Светлая) ☀️ |
|---------|----------------------|----------------------|
| **Фон страницы** | `#511809` espresso | `#fcfada` light-yellow |
| **Карточки** | `#fcfada` светлые | `#f99f37` золотые |
| **Альт. карточки** | Нет | `#c95240` медно-красные |
| **Текст** | `#fcfada` светлый | `#511809` тёмный |
| **Header** | `rgba(0,0,0,0.5)` | `#511809` espresso |
| **Footer** | `rgba(0,0,0,0.8)` | `#511809` espresso |
| **Атмосфера** | Элегантная, премиум | Энергичная, солнечная |

---

## 💡 Быстрое переключение

Для быстрого переключения между темами можно создать два отдельных CSS файла:
- `styles-dark.css` (Вариант 1)
- `styles-light.css` (Вариант 2)

И переключать их в `src/index.tsx`:
```tsx
<link href="/static/styles-light.css" rel="stylesheet">
```

---

**Последнее обновление**: 26.12.2025  
**Текущая активная тема**: ☀️ Вариант 2 - Светлая
