# PHP Админ-панель для "Время Человека"

Простая и безопасная админ-панель для управления проектами и новостями статичного сайта на Astro.

## Возможности

- ✅ Создание и редактирование проектов
- ✅ Создание и редактирование новостей
- ✅ Защита паролем через .htaccess
- ✅ Автоматическое создание slug из заголовков
- ✅ Поддержка Markdown разметки
- ✅ Адаптивный дизайн
- ✅ Простой и понятный интерфейс

## Требования

- PHP 8.3+ (у вас установлен PHP 8.3.27)
- Apache с mod_rewrite
- Права на запись в директории `src/content/projects` и `src/content/news`

## ⚠️ ВАЖНО: Подготовка хостинга

**Перед установкой админ-панели необходимо развернуть структуру директорий с контентом на хостинге!**

На хостинге находится только статическая версия сайта (HTML-файлы), но админка работает с исходными Markdown-файлами в директориях `src/content/`.

**📖 Читайте подробную инструкцию: [DEPLOYMENT.md](DEPLOYMENT.md)**

Кратко:
1. Создайте на сервере директории `src/content/projects`, `src/content/news` и т.д.
2. Загрузите туда существующие `.md` и `.json` файлы из локального проекта
3. Установите права доступа (chmod 755)
4. Только после этого устанавливайте админ-панель

## Установка админ-панели

### 1. Загрузка файлов на сервер

Загрузите всю папку `admin-php` на ваш сервер в корень сайта:

```
/var/www/u0557545/data/www/vremyacheloveka.ru/admin-php/
```

### 2. Создание файла с паролем

Для создания файла `.htpasswd` с паролем выполните на сервере:

```bash
cd /var/www/u0557545/data/www/vremyacheloveka.ru/admin-php/
htpasswd -c .htpasswd admin
```

Вам будет предложено ввести пароль дважды. Используйте надёжный пароль!

**Важно:** Если команда `htpasswd` не найдена, можете использовать онлайн-генератор:
- Перейдите на https://hostingcanada.org/htpasswd-generator/
- Введите логин: `admin`
- Введите желаемый пароль
- Скопируйте результат в файл `.htpasswd` вручную

### 3. Установка прав доступа

```bash
# Права на директорию админки
chmod 755 /var/www/u0557545/data/www/vremyacheloveka.ru/admin-php/

# Защита файла паролей
chmod 644 /var/www/u0557545/data/www/vremyacheloveka.ru/admin-php/.htpasswd

# Права на запись для директорий с контентом
chmod 755 /var/www/u0557545/data/www/vremyacheloveka.ru/src/content/projects/
chmod 755 /var/www/u0557545/data/www/vremyacheloveka.ru/src/content/news/
```

### 4. Проверка путей

Откройте файл `config.php` и убедитесь, что пути указаны правильно:

```php
define('PROJECTS_DIR', __DIR__ . '/../src/content/projects');
define('NEWS_DIR', __DIR__ . '/../src/content/news');
```

## Доступ к админ-панели

После установки админ-панель будет доступна по адресу:

```
https://vremyacheloveka.ru/admin-php/
```

При первом входе браузер запросит логин и пароль:
- **Логин:** admin
- **Пароль:** тот, который вы задали при создании .htpasswd

## Использование

### Создание проекта

1. Войдите в админ-панель
2. Нажмите "Проекты" в верхнем меню
3. Нажмите кнопку "+ Создать"
4. Заполните все обязательные поля:
   - Название проекта
   - Краткое описание
   - Категория
   - Статус
   - URL изображения
   - Дата публикации
   - Содержание (в формате Markdown)
5. Нажмите "Сохранить"

### Создание новости

1. Войдите в админ-панель
2. Нажмите "Новости" в верхнем меню
3. Нажмите кнопку "+ Создать"
4. Заполните обязательные поля:
   - Название новости
   - Краткое описание
   - Дата публикации
   - Содержание (в формате Markdown)
5. При необходимости привяжите новость к проекту через поле "Slug проекта"
6. Нажмите "Сохранить"

### Редактирование

В списке проектов или новостей нажмите кнопку "Редактировать" рядом с нужным элементом.

### Удаление

В списке нажмите кнопку "Удалить". Система попросит подтверждение перед удалением.

## Структура файлов

```
admin-php/
├── .htaccess          # Настройки Apache и защита паролем
├── .htpasswd          # Файл с паролями (создаётся вручную)
├── config.php         # Конфигурация и вспомогательные функции
├── index.php          # Главный файл админки
├── dashboard.php      # Дашборд с общей информацией
├── list.php           # Список проектов/новостей
├── edit.php           # Создание и редактирование
├── delete.php         # Удаление элементов
├── styles.css         # CSS стили
└── README.md          # Эта инструкция
```

## Безопасность

- ✅ Защита паролем через Apache .htaccess
- ✅ Защита от XSS атак
- ✅ Валидация входных данных
- ✅ Безопасная работа с файловой системой
- ✅ Защищённые HTTP заголовки

**Рекомендации:**
1. Используйте надёжный пароль (минимум 12 символов, буквы, цифры, спецсимволы)
2. Регулярно меняйте пароль
3. Не используйте админку через незащищённые Wi-Fi сети
4. Рекомендуется использовать HTTPS

## Смена пароля

Для смены пароля выполните:

```bash
cd /var/www/u0557545/data/www/vremyacheloveka.ru/admin-php/
htpasswd .htpasswd admin
```

## Устранение неполадок

### Ошибка 500 Internal Server Error

1. Проверьте права доступа к файлам
2. Убедитесь, что путь в `AuthUserFile` в `.htaccess` указан правильно
3. Проверьте логи Apache: `/var/log/apache2/error.log`

### Не сохраняются файлы

1. Проверьте права на запись в директории `src/content/projects` и `src/content/news`
2. Убедитесь, что PHP имеет доступ к этим директориям

### Не работают кириллические названия

Убедитесь, что в настройках Apache включена UTF-8 кодировка.

## Поддержка

По вопросам работы админ-панели обращайтесь к разработчику.

## Лицензия

Разработано специально для фонда "Время Человека"