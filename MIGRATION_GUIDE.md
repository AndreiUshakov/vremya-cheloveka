# 🚀 Руководство по миграции на Astro

## Быстрый старт (5 минут)

### Шаг 1: Переключение на Astro конфигурацию

```bash
# Сохраняем старую конфигурацию
mv package.json package-cloudflare.json
mv tsconfig.json tsconfig-cloudflare.json

# Активируем Astro конфигурацию
mv package-astro.json package.json
mv tsconfig-astro.json tsconfig.json
```

### Шаг 2: Установка зависимостей

```bash
# Удаляем старые зависимости
rm -rf node_modules package-lock.json

# Устанавливаем Astro
npm install
```

### Шаг 3: Запуск локально

```bash
# Запуск dev-сервера
npm run dev

# Откройте http://localhost:4321
# Админка: http://localhost:4321/admin
```

### Шаг 4: Проверка работы

Убедитесь, что все работает:
- ✅ Главная страница загружается
- ✅ Проекты отображаются
- ✅ Страницы проектов открываются
- ✅ Видео в Hero-секции проигрывается
- ✅ Стили применены корректно

## 📦 Деплой на Netlify

### Вариант A: Через интерфейс Netlify

1. **Создайте репозиторий на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Миграция на Astro"
   git branch -M main
   git remote add origin https://github.com/ваш-username/vremya-cheloveka.git
   git push -u origin main
   ```

2. **В Netlify Dashboard**:
   - Войдите на https://app.netlify.com
   - "Add new site" → "Import an existing project"
   - Выберите GitHub → найдите репозиторий
   - **Build settings**:
     - Build command: `npm run build`
     - Publish directory: `dist`
   - Deploy site!

3. **Настройте Netlify Identity** (для админки):
   - Site settings → Identity → Enable Identity
   - Registration preferences → Invite only
   - External providers → Add provider → GitHub
   - Services → Git Gateway → Enable Git Gateway

4. **Пригласите администратора**:
   - Identity → Invite users
   - Введите ваш email
   - Получите приглашение → Accept
   - Теперь можете войти на `https://ваш-сайт.netlify.app/admin`

### Вариант B: Через Netlify CLI

```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Войдите в аккаунт
netlify login

# Инициализируйте проект
netlify init

# Деплой
netlify deploy --prod
```

## 🎨 Управление контентом

### Через Decap CMS (рекомендуется)

1. Перейдите на `/admin` вашего сайта
2. Войдите через Netlify Identity
3. Создавайте/редактируйте контент в визуальном редакторе
4. Изменения автоматически коммитятся в Git и пересобирают сайт

### Через Git/Markdown (для продвинутых)

```bash
# Создать новый проект
cat > src/content/projects/novyj-proekt.md << 'EOF'
---
title: "Новый проект"
slug: "novyj-proekt"
shortDescription: "Описание проекта"
category: "social"
status: "active"
targetAmount: 500000
collectedAmount: 0
beneficiariesCount: 0
regions: ["Москва"]
imageUrl: "/images/novyj-proekt.jpg"
publishedAt: 2024-12-01
---

## О проекте

Детальное описание проекта...
EOF

# Закоммитить и запушить
git add src/content/projects/novyj-proekt.md
git commit -m "Добавлен новый проект"
git push

# Сайт автоматически пересоберется
```

## 🔧 Кастомизация

### Изменить стили

Файл `src/styles/styles.css` содержит все стили sovietwave. Можно менять:
- Цветовую палитру (переменные CSS)
- Шрифты
- Размеры и отступы

### Добавить новую страницу

```astro
---
// src/pages/new-page.astro
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Новая страница">
    <section class="section">
        <div class="container">
            <h1>Новая страница</h1>
        </div>
    </section>
</BaseLayout>
```

### Изменить структуру контента

Редактируйте:
- `src/content/config.ts` - схема коллекций
- `public/admin/config.yml` - поля в CMS

## 📊 Сравнение со старой версией

| Параметр | Cloudflare Stack | Astro Static |
|----------|------------------|--------------|
| **Скорость загрузки** | ~500ms | ~50ms |
| **Стоимость хостинга** | $5-10/мес | $0 (бесплатно) |
| **Сложность деплоя** | Средняя | Простая |
| **Админка** | Кастомная | Готовая (Decap CMS) |
| **Управление контентом** | SQL запросы | Markdown/Git |
| **Масштабируемость** | Ограничена Workers | Безлимитная (CDN) |
| **Надёжность** | 99.9% | 99.99% |

## ⚠️ Важные замечания

### Что работает по-другому

1. **Нет серверной логики**
   - Формы обратной связи нужно интегрировать через Netlify Forms
   - Робокасса интегрируется через iframe/виджет

2. **Контент статичный**
   - Изменения не появляются мгновенно
   - Требуется пересборка сайта (автоматическая при git push)

3. **Аутентификация**
   - Админка доступна только через Netlify Identity
   - Нужно приглашать пользователей вручную

### Миграция видео

Видео `hero-video.mp4` (898KB) уже в `public/static/`:
- Автоматически копируется в `dist/` при сборке
- Доступно по пути `/static/hero-video.mp4`

### Миграция изображений

Изображения проектов используют Unsplash (внешние ссылки):
- Для production рекомендуется скачать и разместить локально
- Создайте папку `public/images/projects/`
- Обновите пути в markdown файлах

## 🐛 Решение проблем

### Ошибка "Cannot find module 'astro:content'"

```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Стили не применяются

Проверьте путь в `BaseLayout.astro`:
```html
<link href="/src/styles/styles.css" rel="stylesheet">
```

### Админка не открывается

1. Убедитесь, что Netlify Identity включен
2. Проверьте `public/admin/config.yml`
3. Очистите кеш браузера

### Изображения не загружаются

Положите изображения в `public/images/`:
```bash
mkdir -p public/images/uploads
# Файлы из public/ доступны как /images/uploads/file.jpg
```

## 📞 Поддержка

Если возникли вопросы:
1. Читайте [README-ASTRO.md](./README-ASTRO.md)
2. Проверьте [Astro Docs](https://docs.astro.build)
3. Смотрите [Decap CMS Docs](https://decapcms.org/docs)

## ✅ Чеклист миграции

- [ ] Переключил package.json и tsconfig.json
- [ ] Установил зависимости (`npm install`)
- [ ] Запустил локально (`npm run dev`)
- [ ] Проверил все страницы работают
- [ ] Создал репозиторий на GitHub
- [ ] Задеплоил на Netlify
- [ ] Настроил Netlify Identity
- [ ] Проверил работу админки (`/admin`)
- [ ] Протестировал создание контента
- [ ] Настроил кастомный домен (опционально)

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ⚡ Быстрый статический сайт
- 💰 Бесплатный хостинг
- 🎨 Визуальная админка
- 📝 Простое управление контентом через Markdown
- 🚀 Автоматический деплой при изменениях